package com.videograbber.app.core

// The headless-WebView approach was replaced by the visible KwaiCaptureActivity
// (a background WebView doesn't autoplay, so the video's mp4 was never fetched).
// Intentionally left empty.
